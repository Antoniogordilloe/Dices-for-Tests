{
  "devDependencies": {
    "@cucumber/cucumber": "^9.2.0",
    "@cucumber/pretty-formatter": "^1.0.0",
    "@playwright/test": "^1.35.1"
  },
  "scripts": {
    "lint": "standard",
    "test": "cucumber-js --require cucumber.conf.js --require tests/steps/**/*.steps.js --format @cucumber/pretty-formatter --format html:tests/reports/cucumber-report.html --tags \"not @Manual\" --parallel 1"
  },
  "dependencies": {
    "playwright": "^1.35.1"
  },
  "name": "Dices",
  "version": "1.0.0",
  "description": "Roll 3 dices. Different types to choose",
  "main": "randomNumbers.js",
  "author": "Antonio Gordillo",
  "license": "MIT"
}
